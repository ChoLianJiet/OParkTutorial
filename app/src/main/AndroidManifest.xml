<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.opark.opark">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--this part i duno which activity to put, cuz CardContainerView and CardStackView are FrameLayouts-->
        <!--if replace <activity></activity> to <framelayout></framelayout>, then can liao, but also cannot run the app-->
        <!--when press liao button will crash-->
        <activity android:name="com.opark.opark.CardSwipeView.internal.CardContainerView">
        </activity>
    </application>

</manifest>